---
name: wordcount
title: Word Count (LIWC-style)
menuGroup: Text
version: '1.0.0'
jas: '1.2'

options:
    - name: data
      type: Data

    - name: textVar
      title: Text Variable
      type: Variable
      permitted: [id, factor]

    - name: lexicon
      title: 'Lexicon: category and its terms'
      type: Array
      default:
        - category: Positivity
          terms: very, good, happy, amazing
        - category: Negations
          terms: not, non, never, no, niente
        - category: Modality
          terms: can*, could*, might*, may*, should*
      template:
        type: Group
        elements:
          - name: category
            type: String
            title: Category
          - name: terms
            type: String
            title: 'Terms (comma-separated)'
      description: |
        One category per row with its terms as a comma-separated list.
        A trailing * marks a prefix wildcard; multi-word terms are allowed.
        The default rows are a working example: edit or remove them.

    - name: detectedWords
      title: Include detected words columns
      type: Bool
      default: false

    - name: saveResults
      title: Append results to the data set
      type: Output
